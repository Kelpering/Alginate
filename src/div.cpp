/**
 * 
 */
#include "Alginate.hpp"

void AlgInt::div(const AlgInt& x, const AlgInt& y, AlgInt& quotient)
{
    // Pretty complex, see Alginate.cpp
}

void AlgInt::div(const AlgInt& x, const AlgInt& y, AlgInt& quotient, AlgInt& remainder)
{
    // Pretty complex, see Alginate.cpp
    // Combined div (returns remainder)
}

uint32_t AlgInt::div_short(const AlgInt& x, uint32_t y, AlgInt& quotient)
{
    // Pretty complex, see Alginate.cpp
    // remainder is never greater than y
}