/**
 * 
 */
#include "Alginate.hpp"

void AlgInt::sub(const AlgInt& x, const AlgInt& y, AlgInt& ret)
{
    // Handle sign (maybe a sign func?)

    // Check for size (to prevent carry issues)

    // Loop block

    return;
}

void AlgInt::sub_short(const AlgInt& x, uint32_t y, AlgInt& ret)
{
    // Handle sign (maybe a sign func?)

    // Check for size (to prevent carry issues)

    // Single digit subtraction

    // Carry block

    return;
}